export const startFlip = new Event("flip.start", { bubbles: true });
export const resetFlip = new Event("flip.reset", { bubbles: true });

export const debugFlip = new Event("flip.debug", { bubbles: true });
